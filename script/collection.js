/**
 * Liste des editions (objets DOM : span)
 * Constante globale initialisé au chargement du script, ne pas modifier.
 */
var G_EDITIONS = $(".edition-name") ;

/**
 * Sauvegarde de l'état initial de la zone de chargement.
 * Constante globale initialisé au chargement du script, ne pas modifier.
 */
var G_INITIAL_CONTENT_SAVE = $("#loadable-content").clone() ;

